# container:
#   image: public/iotedge/iotedgedev:latest
#   endpoint: iotedgetools_dockercr_sc

pool:
  vmImage: 'ubuntu-18.04'

container: mcr.microsoft.com/iotedge/iotedgedev:latest

steps:
  - script: |
      iotedgedev --version 